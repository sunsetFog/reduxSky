//  用户名验证
export const USERNAME_REG = /^[0-9a-zA-Z]{6,16}$/;

// 手机号
export const PHONE_REG = /^1[0-9]{10}$/;

// 密码
export const PASSWORD_REG = /^[0-9a-zA-Z]{6,16}$/;

// 验证码
export const CODE_REG = /^[a-zA-z0-9]{6}$/;

// 验证码
export const NUMBER_REG = /^\+?[1-9][0-9]*$/;
